<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="COVID19"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisplayInserirTestes"></activity>
        <activity android:name=".DisplayMostrar" />
        <activity android:name=".DisplayVerEstatisticas" />
        <activity android:name=".DisplayInserirDoentes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <provider
    android:name="ContentProvider"
    android:authorities="com.example.projeto_final"
    android:exported="false" />
    </application>

</manifest>